# Stocks Helper - Environment Variables
#
# Copy this file to .env and fill in your actual API keys
# cp .env.example .env
#
# Never commit .env to version control - it's in .gitignore

# =============================================================================
# Alpha Vantage API Configuration
# =============================================================================
# Get your free API key at: https://www.alphavantage.co/support/#api-key
# Used for:
#   - Stock market data (prices, historical data)
#   - Fundamental analysis (financial metrics)
# Free tier: 25 API requests per day, 5 requests per minute
ALPHA_VANTAGE_API_KEY=your_alpha_vantage_api_key_here

# =============================================================================
# Exchange Rate API Configuration
# =============================================================================
# Get your free API key at: https://www.exchangerate-api.com/
# Used for:
#   - Currency conversion between different stock markets
#   - Portfolio value calculations in user's preferred currency
# Free tier: 1,500 requests per month
EXCHANGE_RATE_API_KEY=your_exchange_rate_api_key_here

# =============================================================================
# Optional Configuration
# =============================================================================
# Uncomment and modify these if you need to override defaults

# Database path (default: ~/.stocks-helper/stocks.db)
# DB_PATH=/path/to/your/database.db

# Cache directory (default: ~/.stocks-helper/cache)
# CACHE_DIR=/path/to/cache/directory

# Log level (default: INFO, options: DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# Log file path (default: ~/.stocks-helper/stocks-helper.log)
# LOG_FILE=/path/to/logfile.log
